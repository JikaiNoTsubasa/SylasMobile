import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.coregeek.mobile',
  appName: 'SylasMobile',
  webDir: 'dist/sylas-mobile/browser'
};

export default config;
